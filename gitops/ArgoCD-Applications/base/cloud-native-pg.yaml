apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloud-native-pg
  namespace: openshift-gitops
spec:
  destination:
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    path: gitops/Operators/CloudNativePG/operator
    repoURL: 'GITREPO'
    targetRevision: REVISION
  syncPolicy:
    automated:
      selfHeal: true